\documentclass[12pt, titlepage]{article}

\usepackage{amsmath, mathtools}

\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{gensymb}
\usepackage{colortbl}
\usepackage{xr}
\usepackage{hyperref}
\usepackage{longtable}
\usepackage{xfrac}
\usepackage{tabularx}
\usepackage{float}
\usepackage{siunitx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage[section]{placeins}
\usepackage{caption}
\usepackage{fullpage}
\usepackage{cite}
\usepackage{breqn}

\input{../../Comments}
\input{../../Common}

\externaldocument[SRS-]{../../SRS/SRS}

\begin{document}

\title{Hardware Report\\\progname}

\author{\authname}

\date{}

\maketitle

\pagenumbering{roman}

\newpage
\section{Revision History}

\begin{table}[hp]
\caption{Revision History} \label{TblRevisionHistory}
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Date} & \textbf{Developer(s)} & \textbf{Change}\\
\midrule
Dec 21, 2025 & Sathurshan, Omar & Report outline.\\
April 5, 2026 & Sathurshan, Omar & Hardware report completed.\\
\bottomrule
\end{tabularx}
\end{table}

\setlength{\parindent}{0pt}

\newpage

\section{Symbols, Abbreviations and Acronyms}

\begin{table}[h]
  \centering
  \begin{tabular}{|l|l|} 
    \hline		
    \textbf{symbol} & \textbf{description}\\
    \hline
    $\theta_e$ & Error angle \\ \hline
    \progname & 360 Audio analysis system on smart glasses\\ \hline
    CI & Continuous Integration\\ \hline
    DCT & Discrete Cosine Transform \\ \hline
    DOA & Direction of Arrival \\ \hline
    DMA & Direct Memory Access \\ \hline
    FFT & Fast Fourier Transform \\ \hline
    FR & Functional Requirement\\ \hline
    GND & Ground \\ \hline
    HAL & Hardware Abstraction Layer \\ \hline
    HUD & Heads Up Display \\ \hline
    I2S & Inter-Integrated Circuit Sound \\ \hline
    LDA & Linear Discriminant Analysis \\ \hline
    M & Module \\ \hline
    MSB & Most Significant Bit \\ \hline
    NFR & Non-Functional Requirement\\ \hline
    PCA & Principal Component Analysis \\ \hline
    PCB & Printed Circuit Board \\ \hline
    R/Req & Requirement\\ \hline
    SCK & Serial Clock \\ \hline
    SD & Serial Data\\ \hline
    SRS & Software Requirements Specification\\ \hline
    T & Test\\ \hline
    USART & Universal Synchronous/Asynchronous Receiver/Transmitter \\ \hline
    UT & Unit Test\\ \hline
    VnV & Verification and Validation \\ \hline
    WS & Word Select \\ \hline
  \end{tabular}
  \caption{Table of abbreviations and acronyms used in \progname.}
\end{table}

See SRS Documentation at
\hyperref[SRS-sec:symbols]{Symbols, Abbreviations, and Acronyms}
for a complete table used in \progname.

\newpage

\tableofcontents

\listoftables

\listoffigures

\newpage

\pagenumbering{arabic}

\section{Introduction}
The Audio360 project has a significant embedded systems component that is not
fully addressed by other project deliverables. This report documents the hardware
design decisions made throughout the capstone, from requirements gathering and
component selection through to physical integration and software co-design.
\bigskip

The report is organized as follows. Section~\ref{sec:hardware} derives the
hardware requirements from the system SRS and documents the selection and
specification of each component. Section~\ref{sec:theory} provides the
theoretical background for the I2S communication protocol and the
microphone synchronization strategy used to enable direction-of-arrival
estimation. Section~\ref{sec:integration} describes the physical wiring
of the microphone array and microcontroller, and traces the system through
four progressive hardware integration phases. Finally,
Section~\ref{sec:hw_sw} discusses how hardware constraints shaped key
software architecture and implementation decisions, including the
introduction of a hardware abstraction layer.


\section{Hardware}
\label{sec:hardware}

\subsection{Hardware Requirements}
The following requirements from the SRS are applicable to the
embedded hardware:

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{|p{3.5cm}|>{\raggedright\arraybackslash}X|p{3.5cm}|}
        \hline
        \textbf{Requirement} & \textbf{Description} & \textbf{Component}\\ \hline
        FR1.2 & The firmware shall synchronize audio signals from \textbf{all microphones} & Microcontroller and Microphones\\ \hline
        NFR1.2 & The firmware shall operate at a rate faster than 16 kHz sample rate of the microphones & Microcontroller\\ \hline
        FR7.2 & The microphones shall be sensitive to audio waves within the most audible human hearing range (250 Hz - 8 kHz). & Microphones \\ \hline
        NFR7.1 & The microphones shall be capable of sampling at 16 kHz (2x 8kHz as per Nyquist Theorem). & Microcontroller and Microphones\\ \hline
        FR 8.1 - 8.2 + NFR 8.1 & The display shall be capable of displaying information to the user without delay & Display Component \\ \hline
        FR 9.1 + NFR 9.1 - 9.2 + FR 3.4 & MCU shall have a sufficient compute to support sampling and processing 4 microphones at 16 kHz each & Microcontroller \\ \hline
        E.3 & Maximum product budget of \$500. Components should fit in a small form factor and be viable for outdoor usage.& Every component\\ \hline
    \end{tabularx}
    \caption{Requirements from SRS applicable to hardware.}
    \label{table:hw_req_srs}
\end{table}

\subsection{Required Hardware Components}
Based on the described requirements in table \ref{table:hw_req_srs}, the project 
requires the following hardware components:

\begin{itemize}
    \item A processor with sufficient compute and audio peripherals to handle
    four individual time synchronized audio streams.
    \item Microphone components that are sensitive to the human audible range
    that are capable of being sampled in a synchronized manner.
    \item A display component that is portable, non-intrusive and usable outdoors. 
\end{itemize}

Additionally, the hardware should be capable of being miniaturized enough in the
future to fit in a wearable form factor. Since the hardware is expected to fit
in a wearable form factor, the power draw needs to be sufficiently low such that
it may be powered using a portable power source such as a battery (E.3.2).  

\subsection{Hardware Selection Process}


Initial hardware selection considerations focused on meeting the microcontroller
and microphone requirements. There are multiple types of microphones available 
for use in embedded systems, with the two primary categories being analog and
digital microphones. Even digital microphones are in fact analog microphones 
with the analog to digital converter and amplification circuitry built into the
microphone daughter board. Digital microphones are preferred since they 
significantly reduce the electrical design complexity required. This is also
beneficial since the team does not have significant electrical design experience
or knowledge on using operational amplifiers. 

\subsubsection{Microphone Selection}
A quick hardware search for embedded digital microphones yields the INMP441 MEMS 
(micro electrical mechanical system) microphone as the standard choice for most
embedded projects. The microphone outputs samples over the I2S protocol described
in section \ref{sec:i2s_protocol}. Easy to use breakout boards for INMP441
microphones are also readily available. Analog microphones were researched but
avoided as it would require extensive circuit design. 


\subsubsection{Microcontroller Selection}
The microcontroller selection was much more involved since there are many 
options available from multiple brands such as EspressIF, ST Micro Electronics,
and Texas Instruments. MCUs from each brand tend to specialize in different
areas such as media, automotive, and hobby projects. One of the key 
requirements for an MCU for the project is to be able to sample four 
microphones independently but also in sync to ensure that the samples are 
time aligned. This would require the MCU to have four independent I2S 
peripherals which can be synchronized to sample on the same clock edge. 
\bigskip

Each of the major manufacturers have relevant search tools to determine which 
MCU from their product lineup meets requirements. Searching for an MCU with four
independent MCUs on the EspressIF website yields a single result shown in figure
\ref{fig:esp_search}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{diagrams/esp_search.png}
    \caption{Searching for MCU on the EspressIF website.}
    \label{fig:esp_search}
\end{figure}

Investigating the specifications of the I2S peripherals available reveals that 
the ESP32-P4 only has three usable I2S peripherals with the fourth one being low
powered for simple sound detection which makes it unsuitable for the project's 
requirements \cite{esp32_p4}.
\bigskip

Search on the Texas Instruments product selector yields only the MSPM0G5187 
MCU shown in figure \ref{fig:ti_search}, which has inadequate specs for the 
project's use case. The MCU only has two I2S peripherals and 32 kBytes of RAM
which makes it unusable for the project \cite{ti_mspm0}.

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{diagrams/ti_search.png}
    \caption{Searching for MCU on the TI website.}
    \label{fig:ti_search}
\end{figure}

\bigskip
Finally performing the search on the ST Micro product selector page yields more
helpful results shown in figure \ref{fig:stm_search} with specific requirements
such as the CPU clock speed, RAM size and I2S count being selected.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{diagrams/stm_search.png}
    \caption{Searching for MCU on the STM website.}
    \label{fig:stm_search}
\end{figure}

The list then further needs to be narrowed down to MCUs that have development 
kit boards available for purchase. This is not the case for every MCU and even
if the MCU has all the peripherals available, they may not be connected to the
development board pins rendering them unusable. With this criteria the best
suitable microcontroller was deemed to be the STM32F767ZI MCU since it is
available as a 144 pin Nucleo Discovery board with four independent I2S 
interfaces available at the physical pins \cite{digikey_stm32f7}. 

\subsubsection{Output Display}

The output display is responsible for visually notifying the user about the
status of any sound detections and relative direction. Since the system has to
be a portable wearable device with mounting points for the microphones, it is 
reasonable to consider heads up display smart glasses for this purpose. They
offer a transparent display that is visible outdoors with a frame that can be
used to place microphones. Since this is a relatively new technology in the 
consumer segment, there are not many options available. The glasses would also 
require an interface that the system can use to display custom text and graphics.
The top contenders for this type of smart glasses were the Rokid AR Glasses,
Inmo XR Glasses, and the Even Realities G2, all shown in figure \ref{fig:ar_glasses}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{diagrams/different_glasses.png}
    \caption{Left to Right - Rokid Glasses, INMO XR, Even Realities G2}
    \label{fig:ar_glasses}
\end{figure}

At the time of project development, all three products had just been announced
with no shipping guarantees as they were crowd funded projects \cite{rokid_ks,inmo_ks}. 
Based on this information, the Rokid glasses were selected since they were the
cheaper and more lightweight option with a more predictable shipping time of around
December 2025. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{diagrams/rokid_glasses_display.png}
    \caption{Rokid AR Glasses Display}
    \label{fig:rokid_ar_display}
\end{figure}


\subsection{Hardware Specification}
The following subsections will describe the specifications for the selected
hardware.

\subsubsection{STM microcontoller}

The STM32F767ZI MCU has the following relevant specifications \cite{STM_REFERENCE_MANUAL}:

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{p{4cm}|X}
        Specification & Description\\ \hline
        CPU & Single ARM Cortex M7 Core. \\ \hline
        Max Clock & 216 MHz \\ \hline
        SRAM & 512 KB \\ \hline
        Serial Audio Interface & 4x SAI + I2S \\ \hline 
        Flash ROM Memory & 2048 KB \\ \hline
        SPI Interfaces & 6 Quad SPI channels \\ \hline
        Hardware Acceleration & ARM CMSIS \\ \hline
    \end{tabularx}
\end{table}

The four independent serial audio interface peripherals, each with their own
I2S receiver satisfy the project requirements since they can be synchronized to 
sample on a single clock. The overall clock speed is sufficient for required
processing throughput. 512 KB of static RAM is enough memory to store 4 channels
of microphone audio with a significant portion available for processing. The 
MCU also has integrated ARM DSP instructions that can leverage hardware 
acceleration to perform tasks such as fourier transforms.

\subsubsection{Microphone}
The INMP441 microphone has the following relevant specifications \cite{inmp441_datasheet}:

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{p{4cm}|X}
        Specification & Description\\ \hline
        Audio resolution & 24 bits signed data. \\ \hline
        Signal to Noise Ratio & 61 dBA \\ \hline
        Signal Sensitivity & -26 dBFS \\ \hline
        Frequency Response & Flat from 60 Hz to 15 kHz \\ \hline
    \end{tabularx}
\end{table}

The 24 bits of signed data implies 12 bits for the microphone diaphragm extending
outwards and 12 bits for the diaphragm compressing inwards from sound waves in 
the air. The resolution along with the flat frequency response from 60 Hz to 15
kHz makes this microphone an ideal choice for the projects target frequency 
sensitivity range of 20 Hz - 8 kHz (FR 7.2).

\subsubsection{Rokid AR glasses}

The Rokid AI Glasses have the following relevant specifications \cite{RokidGlasses,rokid_ks}:
\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{p{4cm}|X}
        Specification & Description\\ \hline
        Display & Dual Waveguide Optical Displays \\ \hline
        Display Colors & Monochrome Green \\ \hline
        Weight & 49 grams \\ \hline
        Operating System & Android 12L \\ \hline
        External Interfaces & WiFi, Bluetooth, USB 3.0 \\ \hline
    \end{tabularx}
\end{table}

The Rokid AI Glasses run on a standard Android operating system installation
making it easy to deploy a custom Android app which can communicate with the 
whole system over various external interfaces such as USB and Bluetooth. The 
monochrome green displays are projected focused at infinity in front of the user
with the option to put the display in peripheral vision where it is not obstructive
to the user. These specifications thus make the Rokid AI glasses an ideal choice
for this project, especially when considering shipping timelines and cost. 


\label{sec:theory}
\section{Theory}

This section reviews the theoretical background of hardware and software
integration required for this project. It provides the necessary context to
understand the design decisions made by the team. The information presented in
this section comes from Digikey's documentation \cite{I2S} and the 
STM 32 Reference Manual \cite{STM_REFERENCE_MANUAL}.

\subsection{I2S Communication Protocol}
\label{sec:i2s_protocol}

I2S (Inter-Integrated Circuit Sound) is a serial communication protocol designed
specifically for the transmission of digital audio data between integrated
circuits. It enables synchronous and low-latency audio transfer, making it well
suited for applications involving microphones, speakers, and audio devices where
precise timing and synchronization are required. In systems with multiple
microphones, I2S ensures that all devices sample and transmit data using a
common clock, allowing for accurate time alignment of audio signals.
\newline
\newline
\noindent The protocol uses three primary signal lines:
\begin{itemize}
    \item \textbf{SCK (Serial Clock or Bit Clock)}: Controls the timing of each
        transmitted data bit and synchronizes all connected devices.
    \item \textbf{WS (Word Select or Left/Right Clock)}: Indicates whether the
        data being transmitted corresponds to the left or right audio channel.
    \item \textbf{SD (Serial Data)}: Carries the audio sample data.
\end{itemize}

I2S operates using a controller-peripheral (master-slave) architecture. One
device generates the clock signals (SCK and WS) and is referred to as the
controller, while the remaining devices act as peripherals and transmit or
receive audio data accordingly. In this project, the STM32 microcontroller
functions as the controller, generating both the serial clock and word select
signals. The microphones act as peripherals and transmit audio samples on the
SD line in synchronization with these clocks.

Data transmission occurs on either the rising or falling edge of the serial
clock, depending on the configuration of the devices during initialization.
The WS signal changes state once per audio frame, indicating whether the
transmitted data corresponds to the left or right channel. Each audio sample
is aligned with the WS signal such that one complete word is transmitted per
channel per frame. Figure~\ref{fig:i2s_protocol} illustrates the timing
relationship between SCK, WS, and SD.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{diagrams/i2s_protocol.png}
    \caption{I2S protocol as explained from STM32 reference manual.}
    \label{fig:i2s_protocol}
\end{figure}

Audio samples are commonly 16, 24, or 32 bits in length. In this project,
the microphones output 24 bit samples. Since the STM32 microcontroller store
data in 32-bit registers, the received samples require post-processing to
handle proper alignment and padding. This involves shifting the received data.
The microphones transmit data most significant bit (MSB) first, as defined by
the I2S standard. This transmission order is independent of the
microcontroller's internal endianness. Therefore, software must correctly
reconstruct the sample values from the received byte stream. Figure
\ref{fig:i2s_protocol} shows how I2S data is received and stored on the
microcontoller.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{diagrams/i2s_data_transfer.png}
    \caption{I2S data transfer.}
    \label{fig:i2s_data_transfer}
\end{figure}

Figure~\ref{fig:mic} shows the I2S microphone used in this project.
The device provides dedicated pins for the SCK, WS, and SD connections,
allowing direct interfacing with the microcontroller's I2S peripheral.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\linewidth]{../../Images/mic.jpg}
    \caption{I2S microphone.}
    \label{fig:mic}
\end{figure}

\subsection{Micophone Synchronization} \label{sec:mic_sync}

Microphone synchronization ensures that audio samples captured from multiple
microphones correspond to the same point in time. This simultaneity is critical
for this project because direction-of-arrival (DoA) estimation relies on
accurately measuring the time differences at which a sound reaches each
microphone (\hyperref[SRS-FR1_2]{FR1.2}).

The microphones communicate using the I2S protocol and are synchronized through
a shared clock. The microcontroller operates as the I2S master and generates
the SCK and WS signals, while all microphones
operate as slaves. These signals are distributed to each microphone, ensuring
that all devices sample audio simultaneously and remain phase-aligned.
Using a common clock eliminates inter-channel clock drift and minimizes
sampling jitter between microphones. Furthermore, a shared WS signal
guarantees that samples from corresponding channels are aligned consistently in
software buffers for subsequent processing.

Figure~\ref{fig:mic_wiring} illustrates the microphone wiring.
The orange wire represents the SCK signal and the green wire represents the
WS signal. Both signals are common to all microphones in the array.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{wiring/microphone_array.png}
    \caption{Microphone wiring. Note that this diagram does not represent the
        exact microphone array position. The purpose is to illustrate the
        wiring.}
    \label{fig:mic_wiring}
\end{figure}

\section{Hardware Integration}
\label{sec:integration}

\subsection{Wiring}

Figure~\ref{fig:mic_wiring} illustrates the microphone array wiring
configuration. As described in Section~\ref{sec:mic_sync}, the SCK and WS
signals are shared among all microphones to achieve
synchronized sampling. The left/right (L/R) channel signal is identical for all
microphones to ensure that each device stores data within the same channel
frame. In this project, all microphones are storing to left channel
(arbitrarily chosen).

Each microphone has an independent data line (blue wire) that transmits
digitized audio data to the microcontroller. These data lines are connected to
the microcontroller input pins PE6, PE3, PD11, and PA0. Power and ground
connections are supplied to each microphone to ensure proper operation.

A micro-USB cable is used to transmit data from the microcontroller to the
smart glasses. Data is communicated via the USB transmit (TX) interface.
Therefore, the connector on the receiving end may vary, provided that the
connected device operates as a USB host.

\subsection{Verification}

Throughout the development of the capstone project, not all hardware components
were available immediately. The smart glasses were expected to arrive
approximately four months into the project, and the printed circuit board (PCB)
for the glasses could not be designed until the wiring of all hardware
components had been finalized. 

As a result, hardware integration was divided into multiple development phases,
allowing the system design to evolve iteratively while software development and
testing proceeded in parallel. This section presents a chronological overview of
the hardware integration evolution process.

\subsubsection{Phase 1: Microcontroller Bring-Up}
In this phase, the team focused on establishing a development environment for
the microcontroller. The primary objective was to build and execute a simple
\textit{Hello World} application to verify correct compilation, flashing, and
execution on the target hardware. This phase enabled familiarity with the
microcontroller toolchain and development workflow.

USB serial communication was then implemented to transmit log data from the
microcontroller to a debugging laptop. This capability was essential, as it
provided the only means of observing internal system behavior during early
development using the CLion debugger.

\subsubsection{Phase 2: Microphone Integration}

Microphones became available in mid October and were subsequently wired to the
microcontroller. Initial development began with a single microphone to verify
configuration and data acquisition before scaling to a
four microphone synchronized array.

To evaluate microphone performance, an SD card module was added to the
microcontroller, and captured audio data were stored as \texttt{.wav} files for
offline playback and verification. In parallel, the serial communication
infrastructure developed in Phase~1 was reused to stream audio data to a host
computer. A Python script was implemented to visualize microphone frequency
responses in real time and provide live audio playback.

These tools enabled rapid verification of microphone functionality and
facilitated detection of wiring and soldering errors. The resulting microphone
array configuration is illustrated in Figure~\ref{fig:mic_wiring}.

\subsubsection{Phase 3: Glasses Integration and System Validation}

In Phase~3, the microphone array was reconfigured to match the physical
dimensions of the smart glasses, figure
\ref{fig:phase3_hardware}. This allowed direction-of-arrival (DoA)
estimation and sound classification algorithms to be evaluated directly on the
microcontroller which was representative of the final product.

During this phase, the smart glasses hardware arrived, enabling the development
of a custom communication protocol between the microcontroller and the glasses.
With these components integrated, the system achieved full end-to-end
functionality.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{diagrams/phase3_hardware.jpg}
    \caption{Hardware configuration during Phase 3.}
    \label{fig:phase3_hardware}
\end{figure}

\subsubsection{Phase 4: PCB Implementation}

In the final phase, the wiring was transitioned from a breadboard based
prototype to a custom PCB, figure \ref{fig:pcb}. This reduced the risk of loose
connections, and enhanced the overall appearance and robustness of the final
product. Table \ref{tab:pcb_mic_info} summarizes the pin connections from the
microphones to the PCB pin outs to the microcontroller.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{diagrams/pcb.jpg}
    \caption{PCB for attatching microphones on the glasses.}
    \label{fig:pcb}
\end{figure}

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{|X|X|X|X|}
    \hline
    {\bf Mic Number} & {\bf Mic Location} & {\bf PCB Pin Out} & {\bf SAI Pin}\\
    \hline
    1 & Top Left (45$\degree$) & S2 & Block A1\\
    2 & Top Right (135$\degree$) & S3 & Block B1\\
    3 & Bottom Right (225$\degree$) & S4 & Block A2\\
    4 & Bottom Left (305$\degree$) & S1 & Block B2\\
    \hline
    \end{tabularx}
    \caption{Microphone data link connection on PCB. Angles are relative to
    glasses coordinate frame (refer to \hyperref[SRS-FR5_4]{FR5.4} from SRS).}
    \label{tab:pcb_mic_info}
\end{table}

Furthermore, the team changes the microcontroller to glasses communication from
USB to bluetooth. The reason for this upgrade was because the USB connection
was not consistent and the available wire connection to the glasses was short.
Bluetooth gave the team flexibility to connect to the glasses wirelessly with
more consistent connection. Figure \ref{fig:bluetooth_wiring} demonstrates the
wiring for the bluetooth module to the microcontroller.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{wiring/bluetooth_wiring.png}
    \caption{Bluetooth wiring diagram.}
    \label{fig:bluetooth_wiring}
\end{figure}

With all design changes integrated, the final product is shown in figure
\ref{fig:final_product_isometric} and figure \ref{fig:final_product_front}.
The PCB is mounted directly onto a 3D printed frame,
which will directly mount onto the actual glasses. This is to allow a
temporarily installment of the hardware as the team could not install a
permanent solution. Orange wiring is used to organize and route connections
from the PCB to the microcontroller. At this stage, the microcontroller has not
yet been mechanically integrated into the glasses. Given the software focused
nature of the capstone, physical portability was not a primary design priority.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{diagrams/final_product_isometric.png}
    \caption{Final hardware product isometric view.}
    \label{fig:final_product_isometric}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{diagrams/final_product_front.jpg}
    \caption{Final hardware product front view.}
    \label{fig:final_product_front}
\end{figure}

\section{Software-Hardware Integration}
\label{sec:hw_sw}

This section describes how the software was designed in accordance with the
hardware architecture. As software engineering students, it was essential to
account for the hardware's limitations and capabilities, as these constraints
directly influenced the overall software design and implementation decisions.

\subsection{Hardware Abstraction Layer}

At the beginning of the project, there was significant uncertainty regarding
the availability and suitability of the hardware. It was unclear whether the
team would have access to smart glasses for data visualization or a
microcontroller capable of processing audio data in real time. To mitigate
these risks, contingency plans were developed. For example, the smart glasses
could be replaced with a monitor to display user output, or audio data could be
collected on the microcontroller and streamed to a laptop with sufficient
computational resources to execute the \progname{} features.

Given this uncertainty, the decision was made to abstract the hardware from the
software implementation by introducing a hardware abstraction layer (HAL),
figure \ref{fig:system_layer}. A layered software architecture is used to
acheive this as it allows to decouple layers. In this case, the HAL layer
provides a uniform software interface that is independent of the
underlying hardware. Only the hardware specific interfacing code must be
modified if components are replaced, which are typically provided by
manufacturers and require some configuration on the team's end.

The primary benefit of this design choice is improved software portability and
reduced dependency on specific hardware platforms, therefore lowering project
risk in the context of a software engineering capstone. However, this approach
introduces additional complexity in both implementation and maintenance of the
generalized abstraction layer. That means that interfacing code between
software and hardware layer must be generic enough to support easy switching.

The hardware abstraction layer is implemented in $src/hardware\_interface$
folder. STM32 interfacing code is located in $src/lib/STM32CubeF7$ folder.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\linewidth]{diagrams/sys_layer.png}
    \caption{System layer diagram.}
    \label{fig:system_layer}
\end{figure}

\subsection{Software Considerations}

Hardware constraints significantly influenced software design decisions at the
implementation level. In particular, the microcontroller provides approximately
500 KB of usable RAM, which is insufficient for computationally intensive tasks.
As a result, the software was designed to minimize memory usage by reusing
objects by employing references instead of creating unnecessary copies.

With this constraint, all algorithms were required to be lightweight and
memory-efficient. This is the primary reason for the team not implementing
a machine learning model for audio classification or direction of arrival
estimation, as there was insufficient memory to store and execute such models.
Additionally, limited RAM capacity restricted the number of audio samples that
could be processed simultaneously. While a larger number of samples generally
improves signal quality and analysis accuracy, memory limitations constrained
the system to approximately 2000 samples per microphone
(about 125~ms of audio data).

Given the potential for hardware-related failures, the software was designed
with error detection and recovery mechanisms if possible. All operations
susceptible to failure, including memory allocation, configuration, and
hardware initialization, are explicitly checked, and appropriate error codes are
returned and handled accordingly. Without these safeguards, the system would
default to an infinite loop of doing nothing, which degrades user experience.
Incorporating structured error handling also enables more efficient debugging
of system faults during development.

\newpage 

\bibliographystyle{IEEEtran}
\bibliography{../../../refs/References}

\end{document}